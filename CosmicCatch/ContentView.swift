import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Cosmic Catch")
            .font(.largeTitle)
            .fontWeight(.bold)
            .padding()
    }
}

#Preview {
    ContentView()
}
